<%= form_for([@station, @crew]) do |f| %>
  <% if crew.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crew.errors.count, "error") %> prohibited this crew from being saved:</h2>

      <ul>
      <% crew.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :country_code %>
    <%= f.text_field :country_code %>
  </div>

  <div class="field">
    <%= f.label :job_title %>
    <%= f.text_field :job_title %>
  </div>

  <div class="field">
    <%= f.label :station_id %>
    <%= f.text_field :station_id %>
  </div>

  <div class="field">
    <%= f.label :launch_at %>
    <%= f.datetime_select :launch_at %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
